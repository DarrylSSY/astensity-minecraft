########## Horizon Config ##########
# Welcome to Horizon's configuration file.
# Before changing anything, please note that all settings has been tested.
# It is recommended not to change any settings if it is not necessary.
#
# Issue-Tracker: https://github.com/CraftG/Horizon/issues
####################################

settings:
  # Disable drag down feature?
  enable_drag_down: true
  # If drag down feature is disabled, should Horizon use drag back instead?
  drag_back_instead: true

  bungee:
    # Do you want to enable bungee mode?
    enabled: false
    # IP of BungeeCord
    host: 127.0.0.1
    # The same to Horizon config in BungeeCord.
    port: 8888

badpackets:
  cancel_vl: 0
  modules:
    KeepAliveExploit:
      # Do you want to enable the KeepAliveExploit check?
      enabled: true
      vl_addition: 5
    CustomPayloadExploit:
      # Do you want to enable the CustomPayloadExploit check?
      enabled: true
      vl_addition: 5

      check_size: 30000
    NoRotation:
      # Do you want to enable the NoRotation check?
      enabled: true

      # Experiment check. Don't add any vl by default.
      vl_addition: 0
  # Action map
  action:
    8:
      - 'horizon notify &7(&a!&7) &a%player% &7failed &aBadPackets &7(Patchwork - %module%) (&a%fixed_vl%VL&7)'
    14:
      - 'horizon notify &7(&6!&7) &6%player% &7failed &6BadPackets &7(Patchwork - %module%) (&6%fixed_vl%VL&7)'
    20:
      - 'horizon notify &7(&c!&7) &c%player% &7failed &cBadPackets &7(Patchwork - %module%) (&c%fixed_vl%VL&7)'
      - 'horizon kick %player% Bad packets'

fastuse:
  cancel_vl: 12
  modules:
    FastEat:
      # Do you want to enable the FastEat check?
      enabled: true
      vl_addition: 2
      # What delay should be enforced for eating an item?
      # Measured in ms
      min_time: 1300
    FastBow:
      # Do you want to enable the FastBow check?
      enabled: true
      vl_addition: 2
    FastToggle:
      # Do you want to enable the FastToggle check?
      enabled: true
      vl_addition: 4

      # How many times should a player be allowed to toggle use-item in a second?
      max_use_toggles: 40
  # Action map
  action:
    6:
      - 'horizon notify &7(&a!&7) &a%player% &7failed &aFastUse &7(Patchwork - %module%) (&a%fixed_vl%VL&7)'
    9:
      - 'horizon notify &7(&6!&7) &6%player% &7failed &6FastUse &7(Patchwork - %module%) (&6%fixed_vl%VL&7)'
    12:
      - 'horizon notify &7(&c!&7) &c%player% &7failed &cFastUse &7(Patchwork - %module%) (&c%fixed_vl%VL&7)'

fly:
  cancel_vl: 100
  modules:
    Ability:
      # Do you want to enable the Ability check?
      enabled: true
      vl_addition: 5
    Equality:
      # Do you want to enable the Equality check?
      enabled: true
      vl_addition: 3
    Expect:
      # Do you want to enable the Expect check?
      enabled: true
      vl_addition: 2
    Step:
      # Do you want to enable the Step check?
      enabled: true
      vl_addition: 3
  # Action map
  action:
    90:
      - 'horizon notify &7(&a!&7) &a%player% &7failed &aFly &7(Patchwork - %module%) (&a%fixed_vl%VL&7)'
    120:
      - 'horizon notify &7(&6!&7) &6%player% &7failed &6Fly &7(Patchwork - %module%) (&6%fixed_vl%VL&7)'
    160:
      - 'horizon notify &7(&c!&7) &c%player% &7failed &cFly &7(Patchwork - %module%) (&c%fixed_vl%VL&7)'

fastbreak:
  cancel_vl: 2
  modules:
    Limit:
      # Do you want to enable the Limit check?
      enabled: true
      vl_addition: 1
  # Action map
  action:
    12:
      - 'horizon notify &7(&a!&7) &a%player% &7failed &aFastBreak &7(Patchwork) (&a%fixed_vl%VL&7)'
    15:
      - 'horizon notify &7(&6!&7) &6%player% &7failed &6FastBreak &7(Patchwork) (&6%fixed_vl%VL&7)'
    18:
      - 'horizon notify &7(&c!&7) &c%player% &7failed &cFastBreak &7(Patchwork) (&c%fixed_vl%VL&7)'

speed:
  cancel_vl: 150
  modules:
    Expect:
      # Do you want to enable the Expect check?
      enabled: true
      vl_addition: 2

      # Violation level addition for NoSlow check
      noslow_vl_addition: 5
    FastFall:
      # Do you want to enable the Expect check?
      enabled: true
      vl_addition: 3
    Limit:
      # Do you want to enable the Limit check?
      enabled: true
      vl_addition: 2
    Sneak:
      # Do you want to enable the Limit check?
      enabled: true
      vl_addition: 3

      # How many times should a player be allowed to toggle sneak in a second?
      max_sneak_toggles: 20
    Sprint:
      # Do you want to enable the Sprint check?
      enabled: true
      vl_addition: 3
    Strafe:
      # Do you want to enable the Strafe check?
      enabled: true
      vl_addition: 4
    Ladder:
      # Do you want to enable the Ladder check?
      enabled: true
      vl_addition: 3
  # Action map
  action:
    80:
      - 'horizon notify &7(&a!&7) &a%player% &7failed &aSpeed &7(Patchwork - %module%) (&a%fixed_vl%VL&7)'
    120:
      - 'horizon notify &7(&6!&7) &6%player% &7failed &6Speed &7(Patchwork - %module%) (&6%fixed_vl%VL&7)'
    150:
      - 'horizon notify &7(&c!&7) &c%player% &7failed &cSpeed &7(Patchwork - %module%) (&c%fixed_vl%VL&7)'

timer:
  cancel_vl: 100
  modules:
    Analysis:
      # Do you want to enable the Analysis check?
      enabled: true
      vl_addition: 1

      # Measured in nanosecond
      analysis_time: 7000000000

      # Measured in nanosecond
      min_move_interval: 48500000
  # Action map
  action:
    50:
      - 'horizon notify &7(&a!&7) &a%player% &7failed &aTimer &7(Patchwork) (&a%fixed_vl%VL&7)'
    60:
      - 'horizon notify &7(&6!&7) &6%player% &7failed &6Timer &7(Patchwork) (&6%fixed_vl%VL&7)'
    80:
      - 'horizon notify &7(&c!&7) &c%player% &7failed &cTimer &7(Patchwork) (&c%fixed_vl%VL&7)'

cheststealer:
  cancel_vl: 3
  modules:
    Limit:
      # Do you want to enable the Limit check?
      enabled: true
      vl_addition: 2
  # Action map
  action:
    12:
      - 'horizon notify &7(&a!&7) &a%player% &7failed &aChestStealer &7(Patchwork) (&a%fixed_vl%VL&7)'
    15:
      - 'horizon notify &7(&6!&7) &6%player% &7failed &6ChestStealer &7(Patchwork) (&6%fixed_vl%VL&7)'
    20:
      - 'horizon notify &7(&c!&7) &c%player% &7failed &cChestStealer &7(Patchwork) (&c%fixed_vl%VL&7)'

velocity:
  cancel_vl: 1
  modules:
    Expect:
      # Do you want to enable the Expect check?
      enabled: true
      vl_addition: 2
  # Action map
  action:
    16:
      - 'horizon notify &7(&a!&7) &a%player% &7failed &aAnti-Knockback &7(Patchwork - %module%) (&a%fixed_vl%VL&7)'
    25:
      - 'horizon notify &7(&6!&7) &6%player% &7failed &6Anti-Knockback &7(Patchwork - %module%) (&6%fixed_vl%VL&7)'
    35:
      - 'horizon notify &7(&c!&7) &c%player% &7failed &cAnti-Knockback &7(Patchwork - %module%) (&c%fixed_vl%VL&7)'
      - 'horizon kick %player% No knockback'

killaura:
  cancel_vl: 35
  modules:
    Heuristic:
      # Do you want to enable the Heuristic check?
      enabled: true
      vl_addition: 3

      reset_head_pos: true
    Click:
      # Do you want to enable the Click check?
      enabled: true
      vl_addition: 2
    KeepSprint:
      # Do you want to enable the KeepSprint check?
      enabled: true
      vl_addition: 2
    Impossible:
      # Do you want to enable the Impossible check?
      enabled: true
      vl_addition: 4
    Reach:
      # Do you want to enable the Reach check?
      enabled: true
      vl_addition: 2

      max_reach: 3.25
      max_reach_creative: 5.0
    SuperKnockback:
      # Do you want to enable the SuperKnockback check?
      enabled: true
      vl_addition: 6
  # Action map
  action:
    12:
      - 'horizon notify &7(&a!&7) &a%player% &7failed &aKill Aura &7(Patchwork - %module%) (&a%fixed_vl%VL&7)'
    20:
      - 'horizon notify &7(&6!&7) &6%player% &7failed &6Kill Aura &7(Patchwork - %module%) (&6%fixed_vl%VL&7)'
    35:
      - 'horizon notify &7(&c!&7) &c%player% &7failed &cKill Aura &7(Patchwork - %module%) (&c%fixed_vl%VL&7)'

liquidwalk:
  cancel_vl: 5
  modules:
    Hover:
      # Do you want to enable the Hover check?
      enabled: true
      vl_addition: 3
    Jump:
      # Do you want to enable the Jump check?
      enabled: true
      vl_addition: 1
  # Action map
  action:
    20:
      - 'horizon notify &7(&a!&7) &a%player% &7failed &aLiquidWalk &7(Patchwork) (&a%fixed_vl%VL&7)'
    28:
      - 'horizon notify &7(&6!&7) &6%player% &7failed &6LiquidWalk &7(Patchwork) (&6%fixed_vl%VL&7)'
    36:
      - 'horizon notify &7(&c!&7) &c%player% &7failed &cLiquidWalk &7(Patchwork) (&c%fixed_vl%VL&7)'

nofall:
  cancel_vl: 1
  modules:
    GroundSpoof:
      # Do you want to enable the GroundSpoof check?
      enabled: true
      vl_addition: 1
  # Action map
  action:
    15:
      - 'horizon notify &7(&a!&7) &a%player% &7failed &aNoFall &7(Patchwork) (&a%fixed_vl%VL&7)'
    25:
      - 'horizon notify &7(&6!&7) &6%player% &7failed &6NoFall &7(Patchwork) (&6%fixed_vl%VL&7)'
    40:
      - 'horizon notify &7(&c!&7) &c%player% &7failed &cNoFall &7(Patchwork) (&c%fixed_vl%VL&7)'

phase:
  cancel_vl: 0
  modules:
    Collision:
      # Do you want to enable the Collision check?
      enabled: true
      vl_addition: 2
  # Action map
  action:
    20:
      - 'horizon notify &7(&a!&7) &a%player% &7failed &aPhase &7(Patchwork) (&a%fixed_vl%VL&7)'
    35:
      - 'horizon notify &7(&6!&7) &6%player% &7failed &6Phase &7(Patchwork) (&6%fixed_vl%VL&7)'
    50:
      - 'horizon notify &7(&c!&7) &c%player% &7failed &cPhase &7(Patchwork) (&c%fixed_vl%VL&7)'

scaffold:
  cancel_vl: 6
  modules:
    Rotation:
      # Do you want to enable the Rotation check?
      enabled: true
      vl_addition: 2
  # Action map
  action:
    10:
      - 'horizon notify &7(&a!&7) &a%player% &7failed &aScaffold &7(Patchwork) (&a%fixed_vl%VL&7)'
    12:
      - 'horizon notify &7(&6!&7) &6%player% &7failed &6Scaffold &7(Patchwork) (&6%fixed_vl%VL&7)'
    15:
      - 'horizon notify &7(&c!&7) &c%player% &7failed &cScaffold &7(Patchwork) (&c%fixed_vl%VL&7)'

freecam:
  # Unused
  cancel_vl: 0
  modules:
    Packet:
      # Do you want to enable the Packet check?
      enabled: true
      # Unused
      vl_addition: 0
  # Unused action map
  action: {}

esp:
  # Unused
  cancel_vl: 0
  modules:
    StorageESP:
      # Do you want to enable the StorageESP check?
      enabled: false
      # Unused
      vl_addition: 0
      # Check interval
      # Increasing it can save more performance
      # Decreasing it can make the check more accurate
      # Measured in tick
      check_interval: 3

      # Storages within this distance will be shown.
      min_distance: 8

      # Storages outside this distance will not be checked.
      max_distance: 80
    PlayerESP:
      # Do you want to enable the PlayerESP check?
      enabled: false
      # Unused
      vl_addition: 0
      # Check interval
      # Increasing it can save more performance
      # Decreasing it can make the check more accurate
      # Measured in tick
      check_interval: 3

      # Players within this distance will be shown.
      min_distance: 8

      # Players outside this distance will not be checked.
      max_distance: 60
  # Unused action map
  action: {}

healthtag:
  # Unused
  cancel_vl: 0
  modules:
    HealthSpoof:
      # Do you want to enable the HealthSpoof check?
      enabled: true
      # Unused
      vl_addition: 0
    ItemSpoof:
      # Do you want to enable the ItemSpoof check?
      enabled: true
      # Unused
      vl_addition: 0
      # Should Horizon spoof item amount?
      spoof_amount: true
      # Should Horizon spoof item enchantments?
      spoof_enchantments: true
      # Should Horizon spoof item durability?
      # This will also spoof unbreakable flag
      spoof_durability: true
  # Unused action map
  action: {}

bowaimbot:
  # Unused
  cancel_vl: 0
  modules:
    Velocity:
      # Do you want to enable the Velocity check?
      enabled: true
      # Unused
      vl_addition: 0
      # Multiply the velocity of arrows to make hack client failed to expect the arrow's trajectory.
      # It can be harmful if you set it too high.
      multiplier: 1.1
      # The y-axis will be added for arrows.
      # Your players may complain if you set it too high.
      y_addition: 0.1
  # Unused action map
  action: {}

ghostblock:
  # Unused
  cancel_vl: 0
  modules:
    Packet:
      # Do you want to enable the Packet check?
      # This check will update the blocks around the players who failed movement checks.
      enabled: true

      # If player still get flagged after updating blocks,
      # should Horizon not update blocks anymore?
      strict: true
  # Unused action map
  action: {}